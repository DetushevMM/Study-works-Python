# Магический шар 8
# Описание проекта: магический шар 8 (шар судьбы) — шуточный способ предсказывать будущее. Программа должна просить пользователя задать некий вопрос, чтобы случайным образом на него ответить.

# Составляющие проекта:

# - Целые числа (тип int);
# - Переменные;
# - Ввод / вывод данных (функции input() и print());
# - Условный оператор (if/elif/else);
# - Цикл while;
# - Бесконечный цикл;
# - Операторы break, continue;
# - Работа с модулем random для генерации случайных чисел.

from random import choice, randint, uniform
from time import sleep
import sys

# Медленная печать с мистическим эффектом
def typo_print(text, end_sleep=1):
    for char in text:
        sleep(uniform(0.01, 0.05))
        print(char, end='', flush=True)
    sleep(end_sleep)
    print()

# Мистическое приветствие — "шар"
def draw_magic_ball():
    print("""
           _______
         .-       -.
       /           \\
      |  ~  ~   ~   |
      |     8       |
      |  ~     ~    |
       \           /
         '-.___.-'
    """)
    typo_print("🔮 Магический Шар Судьбы приветствует тебя...")

# Ответы в эзотерическом стиле
answers = [
    ['Положительный исход', 
     'Звезды говорят "да"...', 
     'Так суждено.', 
     'Да, путь твой ясен.', 
     'Без сомнений, истина на твоей стороне.', 
     'Ты можешь быть уверен — всё сложится.'],
    
    ['Пока всё к лучшему', 
     'Мне чудится: да...', 
     'Судьба склоняется к "да".', 
     'Небо даёт тебе зелёный свет.', 
     'Предзнаменования благоприятны.', 
     'Да, если не свернёшь с пути.'],

    ['Нейтральный знак', 
     'Тени молчат... Попробуй позже.', 
     'Врата не открылись — ещё не время.', 
     'Тайна ещё не готова быть раскрытой.', 
     'Невозможно сказать... Пока.', 
     'Сконцентрируй помыслы и спроси вновь.'],

    ['Предупреждение', 
     'Нет. И даже не помышляй об этом.', 
     'Мрак сгущается... Отказ.', 
     'По звёздным данным — нет.', 
     'Предвестия тревожны.', 
     'Ответ омрачен — весьма сомнительно.']
]

# Запуск программы
def magic_ball():
    draw_magic_ball()
    typo_print('Я вижу тебя, странник. Как нарекаешь себя?', end_sleep=0.5)
    name = input('> ').strip().capitalize()
    typo_print(f'Добро пожаловать, {name}. Задавай свой вопрос судьбе...')

    while True:
        input('\n🔮 Твой вопрос: ')
        pause = randint(1, 4)
        if pause > 2:
            typo_print('Хм... Тонкий вопрос. Советую сосредоточиться...')
            sleep(pause)

        category = choice(answers)
        response = choice(category[1:])
        typo_print(f'✨ Пророчество: [{category[0]}] — {response}\n')

        typo_print('Желаешь ли узнать судьбу ещё раз? (да/нет)', end_sleep=0.2)
        again = input('> ').lower()
        if again not in ['да', 'lf', 'yes', 'д', 'y']:
            typo_print('\n🌑 Шар уходит в туман... Возвращайся, когда снова возжелаешь познания.')
            break

# Запуск
if __name__ == '__main__':
    magic_ball()
